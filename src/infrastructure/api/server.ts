import dotenv from 'dotenv';
import './graphql'; // Importa a configuração do servidor GraphQL

dotenv.config();
// const port: number = Number(process.env.PORT) || 3000;

// app.listen(port, () => {
//   console.log(`Server is listening on port ${port} `);
// });
